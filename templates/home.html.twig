{% include "base.html.twig" %}
  
{% block content %}
  <div class="flexcat">
      {% for categorie in categories %}
      <a href="{{path('register')}}">
        <div class="cardcat">
            <img src='images/{{categorie.image}}' alt="{{categorie.image}}">
              <p>{{categorie.name}}</p>
        </div>
        </a>
      {% endfor %}
</div>
{% endblock %}
