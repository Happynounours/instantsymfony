{% include "base.html.twig" %}

   {% include "shared/header.html.twig" %}




    {% block body %}

{% include "shared/selectcatall.html.twig" %}

      <div class="flexcat">
      {% for categorie in categories %}
      {# <a href={{ path('one_categorie', {id: categorie.id}) }}> #}
              <div class="cardcat">
            <img src='images/{{categorie.image}}' alt="{{categorie.image}}">
              <p>{{categorie.name}}</p>
        </div>
        </a>
      {% endfor %}
</div>
{% endblock %}

    {% include "shared/footer.html.twig" %}


