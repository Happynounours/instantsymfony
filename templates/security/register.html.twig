{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>REGISTER USER</h1>

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button>Save</button>
    {{ form_end(form) }}
{% endblock %}
